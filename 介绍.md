# 在线任务管理系统

<div align="center">

**一个功能完整的任务管理Web应用程序**

[![Java](https://img.shields.io/badge/Java-17-orange.svg)](https://www.oracle.com/java/)
[![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.2.0-brightgreen.svg)](https://spring.io/projects/spring-boot)
[![React](https://img.shields.io/badge/React-18-blue.svg)](https://reactjs.org/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-blue.svg)](https://www.postgresql.org/)
[![Docker](https://img.shields.io/badge/Docker-Ready-blue.svg)](https://www.docker.com/)

</div>

## 📋 目录

- [项目简介](#项目简介)
- [功能特性](#功能特性)
- [技术栈](#技术栈)
- [系统架构](#系统架构)
- [快速开始](#快速开始)
- [本地开发](#本地开发)
- [API文档](#api文档)
- [项目结构](#项目结构)
- [环境变量配置](#环境变量配置)
- [测试](#测试)
- [部署指南](#部署指南)
- [故障排除](#故障排除)
- [贡献指南](#贡献指南)
- [许可证](#许可证)

## 项目简介

在线任务管理系统是一个基于Web的现代化任务管理应用程序，旨在帮助用户高效地创建、组织和跟踪个人或团队任务。系统采用前后端分离架构，提供直观的用户界面和强大的任务管理功能。

### 核心特点

- 🔐 **安全认证** - 基于JWT的用户认证和授权
- 📝 **任务管理** - 完整的CRUD操作，支持任务详情、优先级、截止日期
- 📂 **列表组织** - 创建自定义列表来组织不同类别的任务
- 🔍 **智能搜索** - 实时搜索任务标题和描述
- 🎯 **高级筛选** - 按状态、优先级、列表筛选任务
- 📊 **数据统计** - 可视化仪表板展示任务完成情况
- 📱 **响应式设计** - 完美适配桌面和移动设备
- 🐳 **容器化部署** - 使用Docker实现一键部署

## 功能特性

### 用户管理
- ✅ 用户注册（邮箱验证、密码强度检查）
- ✅ 用户登录（JWT令牌认证）
- ✅ 安全登出（令牌失效）
- ✅ 密码加密存储（BCrypt）

### 任务管理
- ✅ 创建任务（标题、描述、优先级、截止日期）
- ✅ 编辑任务（更新所有字段）
- ✅ 删除任务（带确认对话框）
- ✅ 任务状态管理（待办、进行中、已完成）
- ✅ 优先级设置（高、中、低）
- ✅ 截止日期管理
- ✅ 任务元数据（创建时间、更新时间）

### 任务列表
- ✅ 创建自定义列表
- ✅ 编辑列表名称
- ✅ 删除列表（任务自动迁移到默认列表）
- ✅ 默认列表保护（不可删除）
- ✅ 列表任务计数

### 搜索与筛选
- ✅ 实时搜索（标题和描述）
- ✅ 按状态筛选
- ✅ 按优先级筛选
- ✅ 按列表筛选
- ✅ 组合筛选支持

### 统计与分析
- ✅ 总任务数统计
- ✅ 按状态分组统计
- ✅ 今日到期任务
- ✅ 逾期任务提醒
- ✅ 可视化仪表板

## 技术栈

### 后端技术
- **Java 17** - 编程语言
- **Spring Boot 3.2.0** - 应用框架
- **Spring Security** - 安全框架（JWT认证）
- **Spring Data JPA** - 数据持久化
- **PostgreSQL 15** - 关系型数据库
- **Maven** - 项目构建工具
- **Hibernate** - ORM框架
- **Lombok** - 代码简化工具

### 前端技术
- **React 18** - UI框架
- **TypeScript** - 类型安全的JavaScript
- **React Router v6** - 路由管理
- **Axios** - HTTP客户端
- **CSS3** - 样式设计
- **React Hooks** - 状态管理

### 数据库
- **PostgreSQL 15** - 主数据库
- **JPA/Hibernate** - ORM映射

### DevOps
- **Docker** - 容器化
- **Docker Compose** - 多容器编排
- **Nginx** - 前端Web服务器
- **Maven** - 后端构建
- **npm** - 前端包管理

## 系统架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                         用户浏览器                            │
│                    (http://localhost:3000)                   │
└────────────────────────┬────────────────────────────────────┘
                         │ HTTP/HTTPS
                         │ (JWT Token in Headers)
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                      React 前端应用                           │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Components: Pages, Forms, Cards, Filters, etc.     │   │
│  ├──────────────────────────────────────────────────────┤   │
│  │  Services: API calls, Authentication, State Mgmt    │   │
│  ├──────────────────────────────────────────────────────┤   │
│  │  Routing: React Router (Public/Private Routes)      │   │
│  └──────────────────────────────────────────────────────┘   │
│                    (Port 3000 / Nginx:80)                    │
└────────────────────────┬────────────────────────────────────┘
                         │ REST API
                         │ JSON Payload
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   Spring Boot 后端 API                        │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Controllers: Auth, Task, TaskList, Statistics      │   │
│  ├──────────────────────────────────────────────────────┤   │
│  │  Security: JWT Filter, Authentication Entry Point   │   │
│  ├──────────────────────────────────────────────────────┤   │
│  │  Services: Business Logic, Validation, Processing   │   │
│  ├──────────────────────────────────────────────────────┤   │
│  │  Repositories: JPA Data Access Layer                │   │
│  ├──────────────────────────────────────────────────────┤   │
│  │  Models: User, Task, TaskList Entities              │   │
│  └──────────────────────────────────────────────────────┘   │
│                        (Port 8080)                           │
└────────────────────────┬────────────────────────────────────┘
                         │ JDBC
                         │ SQL Queries
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    PostgreSQL 数据库                          │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Tables: users, tasks, task_lists                   │   │
│  ├──────────────────────────────────────────────────────┤   │
│  │  Indexes: user_id, list_id, status, due_date        │   │
│  ├──────────────────────────────────────────────────────┤   │
│  │  Constraints: Foreign Keys, Unique Constraints      │   │
│  └──────────────────────────────────────────────────────┘   │
│                        (Port 5432)                           │
└─────────────────────────────────────────────────────────────┘
```

### 分层架构

```
┌─────────────────────────────────────────────────────────────┐
│                      表现层 (Presentation)                    │
│  - React Components                                          │
│  - UI/UX Logic                                               │
│  - User Interactions                                         │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                      控制层 (Controller)                      │
│  - REST Controllers                                          │
│  - Request Validation                                        │
│  - Response Formatting                                       │
│  - Exception Handling                                        │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                      业务层 (Service)                         │
│  - Business Logic                                            │
│  - Data Validation                                           │
│  - Transaction Management                                    │
│  - Authorization Checks                                      │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    数据访问层 (Repository)                    │
│  - JPA Repositories                                          │
│  - Database Queries                                          │
│  - Entity Mapping                                            │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                      数据库 (Database)                        │
│  - PostgreSQL                                                │
│  - Data Persistence                                          │
└─────────────────────────────────────────────────────────────┘
```

### 数据模型

```
┌─────────────────┐         ┌─────────────────┐         ┌─────────────────┐
│      User       │         │      Task       │         │   TaskList      │
├─────────────────┤         ├─────────────────┤         ├─────────────────┤
│ id (PK)         │1       *│ id (PK)         │*       1│ id (PK)         │
│ username        │◄────────│ title           │────────►│ name            │
│ email (UNIQUE)  │         │ description     │         │ user_id (FK)    │
│ password        │         │ status          │         │ is_default      │
│ created_at      │         │ priority        │         │ created_at      │
└─────────────────┘         │ due_date        │         └─────────────────┘
                            │ user_id (FK)    │
                            │ list_id (FK)    │
                            │ created_at      │
                            │ updated_at      │
                            └─────────────────┘
```

## 快速开始

### 前置要求

在开始之前，请确保您的系统已安装以下软件：

#### 使用Docker部署（推荐）
- [Docker](https://www.docker.com/get-started) (版本 20.10+)
- [Docker Compose](https://docs.docker.com/compose/install/) (版本 2.0+)

#### 本地开发
- [Java JDK 17](https://www.oracle.com/java/technologies/javase/jdk17-archive-downloads.html)
- [Maven 3.9+](https://maven.apache.org/download.cgi)
- [Node.js 18+](https://nodejs.org/) 和 npm
- [PostgreSQL 15](https://www.postgresql.org/download/) (可选，可使用Docker)

### 🚀 使用Docker Compose部署（推荐）

这是最简单快速的部署方式，适合生产环境和快速演示。

#### 步骤 1: 克隆仓库

```bash
git clone <repository-url>
cd task-management-system
```

#### 步骤 2: 配置环境变量（可选）

编辑 `docker-compose.yml` 文件，根据需要修改环境变量：

```yaml
# 数据库配置
POSTGRES_DB: taskmanager
POSTGRES_USER: taskuser
POSTGRES_PASSWORD: taskpass  # 生产环境请修改

# JWT配置
JWT_SECRET: your-secret-key-change-in-production  # 生产环境必须修改
```

#### 步骤 3: 启动所有服务

```bash
# 构建并启动所有容器（后台运行）
docker-compose up -d

# 或者前台运行（查看实时日志）
docker-compose up
```

首次启动会自动：
- 拉取所需的Docker镜像
- 构建前端和后端应用
- 创建数据库和表结构
- 启动所有服务

#### 步骤 4: 验证服务状态

```bash
# 查看所有容器状态
docker-compose ps

# 应该看到三个运行中的容器：
# - task-frontend (前端)
# - task-backend (后端)
# - task-db (数据库)
```

#### 步骤 5: 访问应用

- **前端应用**: http://localhost:3000
- **后端API**: http://localhost:8080/api
- **数据库**: localhost:5432

#### 步骤 6: 创建第一个账户

1. 打开浏览器访问 http://localhost:3000
2. 点击"注册"按钮
3. 填写用户名、邮箱和密码
4. 登录并开始使用！

#### 常用Docker命令

```bash
# 查看日志
docker-compose logs -f

# 查看特定服务的日志
docker-compose logs -f backend
docker-compose logs -f frontend

# 停止所有服务
docker-compose stop

# 停止并删除容器（保留数据）
docker-compose down

# 停止并删除容器和数据卷（清除所有数据）
docker-compose down -v

# 重启服务
docker-compose restart

# 重新构建并启动
docker-compose up -d --build
```

## 本地开发

适合开发人员进行代码调试和功能开发。

### 方式一：完全本地开发

#### 1. 启动PostgreSQL数据库

**选项A：使用Docker（推荐）**
```bash
docker-compose up postgres -d
```

**选项B：使用本地PostgreSQL**
```bash
# 创建数据库
createdb taskmanager

# 或使用psql
psql -U postgres
CREATE DATABASE taskmanager;
```

#### 2. 启动后端服务

```bash
cd backend

# 首次运行：安装依赖
mvn clean install

# 启动Spring Boot应用
mvn spring-boot:run

# 或者使用IDE运行 TaskManagementApplication.java
```

后端服务将在 **http://localhost:8080** 运行

验证后端：
```bash
curl http://localhost:8080/api/auth/login
# 应返回 401 或错误信息（正常，因为未提供凭据）
```

#### 3. 启动前端服务

```bash
cd frontend

# 首次运行：安装依赖
npm install

# 启动开发服务器
npm start
```

前端服务将在 **http://localhost:3000** 运行，浏览器会自动打开。

#### 4. 开始开发

- 前端代码修改会自动热重载
- 后端代码修改需要重启Spring Boot应用（或使用Spring DevTools）

### 方式二：混合开发（推荐）

使用Docker运行数据库，本地运行前后端：

```bash
# 1. 启动数据库
docker-compose up postgres -d

# 2. 在一个终端启动后端
cd backend && mvn spring-boot:run

# 3. 在另一个终端启动前端
cd frontend && npm start
```

### 开发工具推荐

- **IDE**: IntelliJ IDEA (后端) + VS Code (前端)
- **API测试**: Postman 或 Insomnia
- **数据库管理**: DBeaver 或 pgAdmin
- **Git客户端**: GitKraken 或命令行

## 项目结构

```
task-management-system/
│
├── backend/                                    # Spring Boot 后端应用
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/com/taskmanager/
│   │   │   │   ├── config/                   # 配置类
│   │   │   │   │   └── SecurityConfig.java   # Spring Security配置
│   │   │   │   ├── controller/               # REST控制器
│   │   │   │   │   ├── AuthController.java   # 认证端点
│   │   │   │   │   ├── TaskController.java   # 任务端点
│   │   │   │   │   ├── TaskListController.java # 列表端点
│   │   │   │   │   └── StatisticsController.java # 统计端点
│   │   │   │   ├── dto/                      # 数据传输对象
│   │   │   │   │   ├── LoginRequest.java
│   │   │   │   │   ├── RegisterRequest.java
│   │   │   │   │   ├── TaskRequest.java
│   │   │   │   │   ├── TaskResponse.java
│   │   │   │   │   └── ...
│   │   │   │   ├── exception/                # 异常处理
│   │   │   │   │   ├── GlobalExceptionHandler.java
│   │   │   │   │   ├── ResourceNotFoundException.java
│   │   │   │   │   └── ...
│   │   │   │   ├── model/                    # 实体类
│   │   │   │   │   ├── User.java            # 用户实体
│   │   │   │   │   ├── Task.java            # 任务实体
│   │   │   │   │   ├── TaskList.java        # 列表实体
│   │   │   │   │   ├── TaskStatus.java      # 状态枚举
│   │   │   │   │   └── TaskPriority.java    # 优先级枚举
│   │   │   │   ├── repository/               # 数据访问层
│   │   │   │   │   ├── UserRepository.java
│   │   │   │   │   ├── TaskRepository.java
│   │   │   │   │   └── TaskListRepository.java
│   │   │   │   ├── security/                 # 安全组件
│   │   │   │   │   ├── JwtUtil.java         # JWT工具类
│   │   │   │   │   ├── JwtAuthenticationFilter.java
│   │   │   │   │   └── JwtAuthenticationEntryPoint.java
│   │   │   │   ├── service/                  # 业务逻辑层
│   │   │   │   │   ├── AuthService.java     # 认证服务
│   │   │   │   │   ├── TaskService.java     # 任务服务
│   │   │   │   │   ├── TaskListService.java # 列表服务
│   │   │   │   │   └── StatisticsService.java # 统计服务
│   │   │   │   └── TaskManagementApplication.java # 主应用类
│   │   │   └── resources/
│   │   │       ├── application.yml           # 应用配置
│   │   │       └── application-test.yml      # 测试配置
│   │   └── test/                             # 测试代码
│   │       └── java/com/taskmanager/
│   ├── target/                               # 编译输出目录
│   ├── Dockerfile                            # 后端Docker配置
│   └── pom.xml                               # Maven配置文件
│
├── frontend/                                  # React 前端应用
│   ├── public/
│   │   └── index.html                        # HTML模板
│   ├── src/
│   │   ├── components/                       # React组件
│   │   │   ├── ConfirmDialog.tsx            # 确认对话框
│   │   │   ├── EmptyState.tsx               # 空状态提示
│   │   │   ├── ListForm.tsx                 # 列表表单
│   │   │   ├── ListSidebar.tsx              # 列表侧边栏
│   │   │   ├── LoadingSpinner.tsx           # 加载动画
│   │   │   ├── PrivateRoute.tsx             # 路由保护
│   │   │   ├── SearchBar.tsx                # 搜索栏
│   │   │   ├── TaskCard.tsx                 # 任务卡片
│   │   │   ├── TaskFilters.tsx              # 任务筛选器
│   │   │   ├── TaskForm.tsx                 # 任务表单
│   │   │   └── Toast.tsx                    # 提示消息
│   │   ├── contexts/                         # React Context
│   │   │   └── AuthContext.tsx              # 认证上下文
│   │   ├── hooks/                            # 自定义Hooks
│   │   │   └── useToast.tsx                 # Toast Hook
│   │   ├── pages/                            # 页面组件
│   │   │   ├── DashboardPage.tsx            # 仪表板页面
│   │   │   ├── LoginPage.tsx                # 登录页面
│   │   │   ├── RegisterPage.tsx             # 注册页面
│   │   │   └── TasksPage.tsx                # 任务页面
│   │   ├── services/                         # API服务
│   │   │   └── taskService.ts               # 任务API调用
│   │   ├── styles/                           # 样式文件
│   │   │   ├── AuthPages.css                # 认证页面样式
│   │   │   └── responsive.css               # 响应式样式
│   │   ├── types/                            # TypeScript类型
│   │   │   └── task.ts                      # 任务类型定义
│   │   ├── App.tsx                           # 根组件
│   │   ├── App.css                           # 应用样式
│   │   ├── index.tsx                         # 入口文件
│   │   └── index.css                         # 全局样式
│   ├── build/                                # 构建输出目录
│   ├── node_modules/                         # 依赖包
│   ├── .env                                  # 环境变量
│   ├── Dockerfile                            # 前端Docker配置
│   ├── nginx.conf                            # Nginx配置
│   ├── package.json                          # npm配置
│   ├── package-lock.json                     # 依赖锁定文件
│   └── tsconfig.json                         # TypeScript配置
│
├── .kiro/                                     # Kiro规范文档
│   └── specs/
│       └── task-management-system/
│           ├── requirements.md               # 需求文档
│           ├── design.md                     # 设计文档
│           └── tasks.md                      # 任务列表
│
├── docker-compose.yml                         # Docker Compose配置
├── .gitignore                                # Git忽略文件
└── README.md                                 # 项目文档（本文件）
```

### 关键目录说明

#### 后端 (backend/)
- **config/**: Spring配置类，包括安全配置、CORS配置等
- **controller/**: REST API端点，处理HTTP请求
- **dto/**: 数据传输对象，用于API请求和响应
- **exception/**: 自定义异常和全局异常处理器
- **model/**: JPA实体类，映射数据库表
- **repository/**: Spring Data JPA仓库接口
- **security/**: JWT认证相关组件
- **service/**: 业务逻辑实现

#### 前端 (frontend/)
- **components/**: 可复用的React组件
- **contexts/**: React Context API，用于全局状态管理
- **hooks/**: 自定义React Hooks
- **pages/**: 页面级组件
- **services/**: API调用封装
- **styles/**: CSS样式文件
- **types/**: TypeScript类型定义

## API文档

### 基础信息

- **Base URL**: `http://localhost:8080/api`
- **认证方式**: JWT Bearer Token
- **Content-Type**: `application/json`

### 认证流程

1. 注册账户或使用已有账户登录
2. 从登录响应中获取JWT token
3. 在后续请求的Header中添加: `Authorization: Bearer <token>`

---

### 🔐 认证端点

#### 1. 用户注册

**POST** `/api/auth/register`

注册新用户账户。

**请求体**:
```json
{
  "username": "张三",
  "email": "zhangsan@example.com",
  "password": "password123"
}
```

**响应** (201 Created):
```json
{
  "message": "用户注册成功",
  "userId": 1
}
```

**验证规则**:
- 邮箱必须符合标准格式
- 邮箱必须唯一
- 密码长度至少8个字符
- 用户名不能为空

**错误响应**:
```json
{
  "timestamp": "2024-01-15T10:30:00Z",
  "status": 409,
  "error": "Conflict",
  "message": "邮箱已被注册"
}
```

---

#### 2. 用户登录

**POST** `/api/auth/login`

使用邮箱和密码登录。

**请求体**:
```json
{
  "email": "zhangsan@example.com",
  "password": "password123"
}
```

**响应** (200 OK):
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "userId": 1,
  "username": "张三"
}
```

**错误响应** (401 Unauthorized):
```json
{
  "message": "邮箱或密码错误"
}
```

---

#### 3. 用户登出

**POST** `/api/auth/logout`

登出当前用户（使令牌失效）。

**请求头**:
```
Authorization: Bearer <token>
```

**响应** (200 OK):
```json
{
  "message": "登出成功"
}
```

---

### 📝 任务端点

所有任务端点都需要认证（JWT Token）。

#### 1. 获取任务列表

**GET** `/api/tasks`

获取当前用户的所有任务，支持分页、筛选和搜索。

**请求头**:
```
Authorization: Bearer <token>
```

**查询参数**:
| 参数 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| page | integer | 否 | 页码（从0开始） | 0 |
| size | integer | 否 | 每页数量 | 20 |
| status | string | 否 | 任务状态 | TODO, IN_PROGRESS, COMPLETED |
| priority | string | 否 | 优先级 | HIGH, MEDIUM, LOW |
| listId | integer | 否 | 列表ID | 1 |
| search | string | 否 | 搜索关键词 | 会议 |

**示例请求**:
```
GET /api/tasks?page=0&size=20&status=TODO&priority=HIGH&search=项目
```

**响应** (200 OK):
```json
{
  "tasks": [
    {
      "id": 1,
      "title": "完成项目文档",
      "description": "编写API文档和用户手册",
      "status": "TODO",
      "priority": "HIGH",
      "dueDate": "2024-01-20",
      "listId": 1,
      "listName": "工作",
      "createdAt": "2024-01-15T10:00:00Z",
      "updatedAt": "2024-01-15T10:00:00Z"
    }
  ],
  "totalPages": 5,
  "totalElements": 100,
  "currentPage": 0
}
```

---

#### 2. 获取单个任务

**GET** `/api/tasks/{id}`

获取指定任务的详细信息。

**请求头**:
```
Authorization: Bearer <token>
```

**响应** (200 OK):
```json
{
  "id": 1,
  "title": "完成项目文档",
  "description": "编写API文档和用户手册",
  "status": "TODO",
  "priority": "HIGH",
  "dueDate": "2024-01-20",
  "listId": 1,
  "listName": "工作",
  "createdAt": "2024-01-15T10:00:00Z",
  "updatedAt": "2024-01-15T10:00:00Z"
}
```

**错误响应** (404 Not Found):
```json
{
  "message": "任务不存在"
}
```

---

#### 3. 创建任务

**POST** `/api/tasks`

创建新任务。

**请求头**:
```
Authorization: Bearer <token>
Content-Type: application/json
```

**请求体**:
```json
{
  "title": "完成项目文档",
  "description": "编写API文档和用户手册",
  "status": "TODO",
  "priority": "HIGH",
  "dueDate": "2024-01-20",
  "listId": 1
}
```

**字段说明**:
- `title` (必填): 任务标题，不能为空
- `description` (可选): 任务描述
- `status` (可选): 任务状态，默认为 TODO
- `priority` (可选): 优先级，默认为 MEDIUM
- `dueDate` (可选): 截止日期，格式 YYYY-MM-DD
- `listId` (可选): 列表ID，默认使用用户的默认列表

**响应** (201 Created):
```json
{
  "id": 1,
  "title": "完成项目文档",
  "description": "编写API文档和用户手册",
  "status": "TODO",
  "priority": "HIGH",
  "dueDate": "2024-01-20",
  "listId": 1,
  "createdAt": "2024-01-15T10:00:00Z",
  "updatedAt": "2024-01-15T10:00:00Z"
}
```

---

#### 4. 更新任务

**PUT** `/api/tasks/{id}`

更新指定任务的信息。

**请求头**:
```
Authorization: Bearer <token>
Content-Type: application/json
```

**请求体**:
```json
{
  "title": "完成项目文档（已更新）",
  "description": "编写API文档、用户手册和部署指南",
  "status": "IN_PROGRESS",
  "priority": "HIGH",
  "dueDate": "2024-01-25",
  "listId": 1
}
```

**响应** (200 OK):
```json
{
  "id": 1,
  "title": "完成项目文档（已更新）",
  "description": "编写API文档、用户手册和部署指南",
  "status": "IN_PROGRESS",
  "priority": "HIGH",
  "dueDate": "2024-01-25",
  "listId": 1,
  "createdAt": "2024-01-15T10:00:00Z",
  "updatedAt": "2024-01-15T14:30:00Z"
}
```

**错误响应** (403 Forbidden):
```json
{
  "message": "无权限编辑此任务"
}
```

---

#### 5. 删除任务

**DELETE** `/api/tasks/{id}`

删除指定任务。

**请求头**:
```
Authorization: Bearer <token>
```

**响应** (200 OK):
```json
{
  "message": "任务删除成功"
}
```

**错误响应** (403 Forbidden):
```json
{
  "message": "无权限删除此任务"
}
```

---

### 📂 任务列表端点

#### 1. 获取所有列表

**GET** `/api/lists`

获取当前用户的所有任务列表。

**请求头**:
```
Authorization: Bearer <token>
```

**响应** (200 OK):
```json
{
  "lists": [
    {
      "id": 1,
      "name": "我的任务",
      "isDefault": true,
      "taskCount": 15,
      "createdAt": "2024-01-10T08:00:00Z"
    },
    {
      "id": 2,
      "name": "工作",
      "isDefault": false,
      "taskCount": 8,
      "createdAt": "2024-01-12T09:00:00Z"
    }
  ]
}
```

---

#### 2. 获取单个列表

**GET** `/api/lists/{id}`

获取指定列表的详细信息。

**请求头**:
```
Authorization: Bearer <token>
```

**响应** (200 OK):
```json
{
  "id": 1,
  "name": "我的任务",
  "isDefault": true,
  "taskCount": 15,
  "createdAt": "2024-01-10T08:00:00Z"
}
```

---

#### 3. 创建列表

**POST** `/api/lists`

创建新的任务列表。

**请求头**:
```
Authorization: Bearer <token>
Content-Type: application/json
```

**请求体**:
```json
{
  "name": "个人项目"
}
```

**响应** (201 Created):
```json
{
  "id": 3,
  "name": "个人项目",
  "isDefault": false,
  "taskCount": 0,
  "createdAt": "2024-01-15T10:00:00Z"
}
```

---

#### 4. 更新列表

**PUT** `/api/lists/{id}`

更新列表名称。

**请求头**:
```
Authorization: Bearer <token>
Content-Type: application/json
```

**请求体**:
```json
{
  "name": "工作项目"
}
```

**响应** (200 OK):
```json
{
  "id": 2,
  "name": "工作项目",
  "isDefault": false,
  "taskCount": 8,
  "createdAt": "2024-01-12T09:00:00Z"
}
```

---

#### 5. 删除列表

**DELETE** `/api/lists/{id}`

删除指定列表。列表中的任务会自动移动到默认列表。

**请求头**:
```
Authorization: Bearer <token>
```

**响应** (200 OK):
```json
{
  "message": "列表删除成功，任务已移动到默认列表"
}
```

**错误响应** (400 Bad Request):
```json
{
  "message": "不能删除默认列表"
}
```

---

### 📊 统计端点

#### 获取仪表板统计

**GET** `/api/statistics/dashboard`

获取当前用户的任务统计信息。

**请求头**:
```
Authorization: Bearer <token>
```

**响应** (200 OK):
```json
{
  "totalTasks": 50,
  "todoCount": 20,
  "inProgressCount": 15,
  "completedCount": 15,
  "dueTodayCount": 5,
  "overdueCount": 3
}
```

**字段说明**:
- `totalTasks`: 总任务数
- `todoCount`: 待办任务数
- `inProgressCount`: 进行中任务数
- `completedCount`: 已完成任务数
- `dueTodayCount`: 今日到期任务数
- `overdueCount`: 逾期任务数（未完成且截止日期已过）

---

### 错误代码说明

| 状态码 | 说明 | 常见原因 |
|--------|------|----------|
| 400 | Bad Request | 请求参数错误、验证失败 |
| 401 | Unauthorized | 未提供Token或Token无效 |
| 403 | Forbidden | 无权限访问该资源 |
| 404 | Not Found | 资源不存在 |
| 409 | Conflict | 资源冲突（如邮箱已存在） |
| 500 | Internal Server Error | 服务器内部错误 |

### 使用Postman测试

1. 导入API集合（可选）
2. 设置环境变量：
   - `base_url`: http://localhost:8080/api
   - `token`: 登录后获取的JWT token
3. 在请求头中使用 `{{token}}` 变量

## 环境变量配置

### 后端环境变量

在 `backend/src/main/resources/application.yml` 或通过环境变量配置：

| 变量名 | 说明 | 默认值 | 示例 |
|--------|------|--------|------|
| `SPRING_DATASOURCE_URL` | 数据库连接URL | - | `jdbc:postgresql://localhost:5432/taskmanager` |
| `SPRING_DATASOURCE_USERNAME` | 数据库用户名 | - | `taskuser` |
| `SPRING_DATASOURCE_PASSWORD` | 数据库密码 | - | `taskpass` |
| `JWT_SECRET` | JWT签名密钥 | - | `your-secret-key-min-256-bits` |
| `JWT_EXPIRATION` | JWT过期时间（毫秒） | 86400000 | `86400000` (24小时) |
| `SERVER_PORT` | 服务器端口 | 8080 | `8080` |

**application.yml 示例**:
```yaml
spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/taskmanager}
    username: ${SPRING_DATASOURCE_USERNAME:taskuser}
    password: ${SPRING_DATASOURCE_PASSWORD:taskpass}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: ${JWT_SECRET:your-secret-key-change-in-production}
  expiration: ${JWT_EXPIRATION:86400000}

server:
  port: ${SERVER_PORT:8080}
```

### 前端环境变量

在 `frontend/.env` 文件中配置：

| 变量名 | 说明 | 默认值 | 示例 |
|--------|------|--------|------|
| `REACT_APP_API_URL` | 后端API基础URL | - | `http://localhost:8080/api` |

**前端 .env 示例**:
```env
REACT_APP_API_URL=http://localhost:8080/api
```

### Docker环境变量

在 `docker-compose.yml` 中配置：

```yaml
services:
  postgres:
    environment:
      POSTGRES_DB: taskmanager
      POSTGRES_USER: taskuser
      POSTGRES_PASSWORD: taskpass  # 生产环境请修改

  backend:
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/taskmanager
      SPRING_DATASOURCE_USERNAME: taskuser
      SPRING_DATASOURCE_PASSWORD: taskpass
      JWT_SECRET: your-secret-key-change-in-production  # 生产环境必须修改

  frontend:
    environment:
      REACT_APP_API_URL: http://localhost:8080/api
```

### 安全注意事项

⚠️ **重要**: 在生产环境中：
1. 修改所有默认密码
2. 使用强随机字符串作为JWT_SECRET（至少256位）
3. 不要将敏感信息提交到版本控制系统
4. 使用环境变量或密钥管理服务存储敏感配置

## 测试

### 后端测试

项目使用JUnit 5和Spring Boot Test进行测试。

```bash
cd backend

# 运行所有测试
mvn test

# 运行特定测试类
mvn test -Dtest=TaskServiceTest

# 运行测试并生成覆盖率报告
mvn test jacoco:report

# 跳过测试进行构建
mvn clean install -DskipTests
```

**测试类型**:
- **单元测试**: 测试单个类或方法
- **集成测试**: 测试多个组件的交互
- **Repository测试**: 使用@DataJpaTest测试数据访问层
- **Controller测试**: 使用MockMvc测试REST端点

### 前端测试

项目使用Jest和React Testing Library进行测试。

```bash
cd frontend

# 运行所有测试
npm test

# 运行测试并生成覆盖率报告
npm test -- --coverage

# 运行特定测试文件
npm test -- TaskCard.test.tsx

# 监听模式（开发时使用）
npm test -- --watch
```

**测试类型**:
- **组件测试**: 测试React组件的渲染和交互
- **Hook测试**: 测试自定义Hooks
- **集成测试**: 测试多个组件的协作

### 测试覆盖率

查看测试覆盖率报告：

**后端**:
```bash
cd backend
mvn test jacoco:report
# 报告位置: target/site/jacoco/index.html
```

**前端**:
```bash
cd frontend
npm test -- --coverage
# 报告位置: coverage/lcov-report/index.html
```

## 部署指南

### 生产环境部署

#### 1. 准备工作

- 确保服务器已安装Docker和Docker Compose
- 准备域名和SSL证书（可选）
- 配置防火墙规则

#### 2. 克隆代码

```bash
git clone <repository-url>
cd task-management-system
```

#### 3. 配置生产环境变量

创建 `.env.production` 文件：

```env
# 数据库配置
POSTGRES_DB=taskmanager
POSTGRES_USER=taskuser
POSTGRES_PASSWORD=<strong-password>

# JWT配置
JWT_SECRET=<strong-random-secret-min-256-bits>

# API配置
REACT_APP_API_URL=https://api.yourdomain.com/api
```

#### 4. 修改Docker Compose配置

编辑 `docker-compose.yml`，使用生产环境配置：

```yaml
services:
  backend:
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/taskmanager
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
    restart: always

  frontend:
    environment:
      REACT_APP_API_URL: ${REACT_APP_API_URL}
    restart: always
```

#### 5. 启动服务

```bash
# 使用生产环境配置启动
docker-compose --env-file .env.production up -d

# 查看日志
docker-compose logs -f
```

#### 6. 配置反向代理（可选）

使用Nginx作为反向代理：

```nginx
server {
    listen 80;
    server_name yourdomain.com;

    # 前端
    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 后端API
    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

#### 7. 配置SSL（推荐）

使用Let's Encrypt获取免费SSL证书：

```bash
# 安装certbot
sudo apt-get install certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d yourdomain.com
```

### 数据备份

#### 备份数据库

```bash
# 导出数据库
docker exec task-db pg_dump -U taskuser taskmanager > backup.sql

# 恢复数据库
docker exec -i task-db psql -U taskuser taskmanager < backup.sql
```

#### 自动备份脚本

创建 `backup.sh`:

```bash
#!/bin/bash
BACKUP_DIR="/path/to/backups"
DATE=$(date +%Y%m%d_%H%M%S)
docker exec task-db pg_dump -U taskuser taskmanager > "$BACKUP_DIR/backup_$DATE.sql"

# 保留最近7天的备份
find $BACKUP_DIR -name "backup_*.sql" -mtime +7 -delete
```

添加到crontab（每天凌晨2点备份）：
```bash
0 2 * * * /path/to/backup.sh
```

### 监控和日志

#### 查看容器状态

```bash
# 查看所有容器
docker-compose ps

# 查看资源使用情况
docker stats
```

#### 查看日志

```bash
# 查看所有日志
docker-compose logs

# 查看特定服务日志
docker-compose logs backend
docker-compose logs frontend
docker-compose logs postgres

# 实时查看日志
docker-compose logs -f --tail=100
```

#### 日志管理

配置日志轮转，在 `docker-compose.yml` 中添加：

```yaml
services:
  backend:
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
```

## 故障排除

### 常见问题

#### 1. 后端无法连接数据库

**症状**: 后端启动失败，日志显示数据库连接错误

**解决方案**:
```bash
# 检查数据库容器是否运行
docker-compose ps postgres

# 检查数据库日志
docker-compose logs postgres

# 重启数据库
docker-compose restart postgres

# 验证数据库连接
docker exec -it task-db psql -U taskuser -d taskmanager
```

#### 2. 前端无法访问后端API

**症状**: 前端显示网络错误或CORS错误

**解决方案**:
1. 检查 `REACT_APP_API_URL` 环境变量是否正确
2. 检查后端CORS配置
3. 验证后端是否正常运行：
```bash
curl http://localhost:8080/api/auth/login
```

#### 3. JWT Token过期或无效

**症状**: 登录后立即退出或API返回401错误

**解决方案**:
1. 检查JWT_SECRET是否一致
2. 清除浏览器localStorage
3. 检查系统时间是否正确

#### 4. Docker容器启动失败

**症状**: `docker-compose up` 失败

**解决方案**:
```bash
# 清理旧容器和镜像
docker-compose down -v
docker system prune -a

# 重新构建
docker-compose build --no-cache
docker-compose up -d
```

#### 5. 端口被占用

**症状**: 端口3000、8080或5432已被使用

**解决方案**:
```bash
# Windows
netstat -ano | findstr :8080
taskkill /PID <PID> /F

# Linux/Mac
lsof -i :8080
kill -9 <PID>

# 或修改docker-compose.yml中的端口映射
```

#### 6. 数据库迁移问题

**症状**: 表结构不正确或缺失

**解决方案**:
```bash
# 重置数据库（警告：会删除所有数据）
docker-compose down -v
docker-compose up -d

# 或手动运行迁移
docker exec -it task-backend mvn flyway:migrate
```

### 性能优化

#### 后端优化

1. **数据库连接池配置**:
```yaml
spring:
  datasource:
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
```

2. **启用查询缓存**:
```yaml
spring:
  jpa:
    properties:
      hibernate:
        cache:
          use_second_level_cache: true
```

3. **添加数据库索引**（已在设计中包含）

#### 前端优化

1. **启用生产构建**:
```bash
npm run build
```

2. **启用Gzip压缩**（Nginx配置）:
```nginx
gzip on;
gzip_types text/plain text/css application/json application/javascript;
```

3. **使用CDN**（可选）

### 获取帮助

如果遇到问题：

1. 查看日志文件
2. 检查GitHub Issues
3. 查阅相关文档：
   - [Spring Boot文档](https://spring.io/projects/spring-boot)
   - [React文档](https://reactjs.org/)
   - [Docker文档](https://docs.docker.com/)

## 贡献指南

欢迎贡献代码！请遵循以下步骤：

1. Fork本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启Pull Request

### 代码规范

- **后端**: 遵循Java代码规范，使用Checkstyle
- **前端**: 遵循Airbnb JavaScript/React规范
- **提交信息**: 使用清晰的提交信息

### 开发流程

1. 在`.kiro/specs/`中查看需求和设计文档
2. 创建对应的任务分支
3. 编写代码和测试
4. 确保所有测试通过
5. 提交Pull Request

## 许可证

本项目采用 MIT 许可证。详见 [LICENSE](LICENSE) 文件。

## 致谢

- Spring Boot团队
- React团队
- PostgreSQL社区
- 所有贡献者

---

<div align="center">

**如有问题或建议，欢迎提Issue！**

Made with ❤️ by Task Management Team

</div>
